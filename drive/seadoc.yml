services:

  seadoc:
    image: ${SEADOC_IMAGE:-seafileltd/sdoc-server:2.0-latest}
    container_name: seadoc
    restart: unless-stopped
    volumes:
      - ${SEADOC_VOLUME:-/opt/seadoc-data/}:/shared
    extra_hosts:
      - "${SEAFILE_SERVER_HOSTNAME}:172.30.0.10"
    environment:
      - DB_HOST=${SEAFILE_MYSQL_DB_HOST:-db}
      - DB_PORT=${SEAFILE_MYSQL_DB_PORT:-3306}
      - DB_USER=${SEAFILE_MYSQL_DB_USER:-seafile}
      - DB_PASSWORD=${SEAFILE_MYSQL_DB_PASSWORD:?Variable is not set or empty}
      - DB_NAME=${SEADOC_MYSQL_DB_NAME:-${SEAFILE_MYSQL_DB_SEAHUB_DB_NAME:-seahub_db}}
      - TIME_ZONE=${TIME_ZONE:-Etc/UTC}
      - JWT_PRIVATE_KEY=${JWT_PRIVATE_KEY:?Variable is not set or empty}
      - NON_ROOT=${NON_ROOT:-false}
      - SEAHUB_SERVICE_URL=${SEAFILE_SERVICE_URL:-http://seafile}
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    labels:
      caddy: http://${SEAFILE_SERVER_HOSTNAME:?Variable is not set or empty}, https://${SEAFILE_SERVER_HOSTNAME:?Variable is not set or empty}
      caddy.1_handle: "/socket.io/*"
      caddy.1_handle.0_reverse_proxy: "seadoc:80"
      caddy.2_handle_path: "/sdoc-server/*"
      caddy.2_handle_path.0_reverse_proxy: "seadoc:80"
      caddy.tls: internal
    depends_on:
      db:
        condition: service_healthy
    networks:
      - seafile-net

networks:
  seafile-net:
    name: seafile-net
